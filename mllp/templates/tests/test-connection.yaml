apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mllp.fullname" . }}-test-connection"
  labels:
{{ include "mllp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mllp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adminium.fullname" . }}-test-connection"
  labels:
{{ include "adminium.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "adminium.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
